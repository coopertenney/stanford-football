# CollegeFootballData API key. Get one free at https://collegefootballdata.com/key
#
# Every code path reads the key from the environment: src/ingest/cfbd.ts,
# gather_rb_data.py, and the notebook's cell 1. No hardcoded copies remain.
#
# The old hardcoded key was scrubbed BEFORE this repo's first commit, so it never
# entered git history — but it sat in plaintext on disk and was exposed in a
# terminal, so treat it as burned. Get a fresh key; don't try to reuse it.
#
#   export CFBD_API_KEY=...
#   npm run ingest
CFBD_API_KEY=
